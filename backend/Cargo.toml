[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.1"
http = "1.2.0"
serde_json = "1.0.140"
tokio = { version = "1", features = ["full"] }
tower = "0.5.2"
tower-http =  { version = "0.6.2", features = ["cors"] }
# tower = "<latest-version>"