services:
  nginx:
    image: nginx:latest
    container_name: gateway
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/server.conf
    ports:
      - 80:80
      - 443:443
    restart: always

  backend:
    image: backend:latest
    build:
      context: ./
      dockerfile: ./backend/Dockerfile
    restart: always
    